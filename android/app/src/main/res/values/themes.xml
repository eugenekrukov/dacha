<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Базовая тема для SplashScreen / window background -->
    <style name="Theme.DachaCalendar" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/transparent</item>
    </style>
</resources>
