# Статус и бэклог: «Календарь дачника»

> **Этот файл — главный. Читать первым.** Текущий статус + открытые задачи.
>
> **Карта документации:**
> - История сессий — `session-note.md` (новое сверху) + `session-note-archive.md` (старое).
> - Приоритизированный план доработок (техдолг/фиксы → фичи) — `docs/improvement-plan.md`.
> - Отложенные задачи этапа 2+ — `docs/ux-roadmap.md` (единый источник бэклога).
> - Активные планы фич — `docs/superpowers/plans/` + `specs/` (сейчас: offline-today, unified-task-dismiss). Завершённые → `docs/archive/`.
> - Архитектура — `ARCHITECTURE.md`; конвенции — `android/CONVENTIONS.md`, `backend/CONVENTIONS.md`; деплой/ops — `docs/DEPLOY.md`; тесты — `TESTING.md`; инструкции AI — `CLAUDE.md`.
> - Маркетинг/ASO — `docs/promo-plan-first-paying.md`, `docs/gtm-strategy-ru.md`, `docs/aso-*.md`, `docs/master-plan-2026.md`.
> - Конкуренты и позиционирование — `docs/competitors.md` (что подчёркивать на витрине и чего сознательно не делаем).
> - База знаний Obsidian (`Dacha/`) — навигация поверх репо; источник истины всегда репо.

## Текущий статус

- **MVP**: ✅ 100% завершён, в проде. Идёт пост-MVP-разработка.
- **Стек**: Node.js 20 + Fastify 4 + PostgreSQL · Android (Kotlin + Compose + Hilt) · Веб (React + Vite + TS + Tailwind).
- **Бэкенд**: `https://dacha.studio1008.com/` · pm2 `dacha-api`.
- **Веб**: `https://dacha.studio1008.com/app/` (папка `web/`, статика `/var/www/dacha-web`, nginx `location /app/`). Та же БД/API.
- **Android**: package `ru.dachakalend.app` · minSdk 26 · compileSdk/targetSdk **36** (Android 16) · флейворы `rustore`/`gplay` (samsung удалён 2026-06-30) (сборка `:app:compileGplayDebugKotlin` и т.п.).
- **Справочник проблем растений**: в проде на всех платформах (78 записей по факту на 2026-07-02, было
  указано ~68 — актуализировано при аудите `/spravochnik/`). См. `docs/plant-guide-plan.md`.
- **SEO-справочник `/spravochnik/`**: 133 публичные статические страницы (55 культур + 78 проблем),
  в проде. См. «Актуальные открытые задачи» ниже + `session-note.md` (2026-07-02).
- **Блог `/blog/`** (2026-07-18): статьи контент-плана (`docs/vk-content/*.md`) публикуются и на сайте,
  не только в ВК/Telegram/Дзен — `backend/scripts/generate-blog.js`, общие SEO-хелперы с
  spravochnik-генератором вынесены в `backend/scripts/lib/seoPage.js`. Только статьи, ещё не
  опубликованные в ВК (фильтр по дате поста). Ops — `docs/DEPLOY.md` («Блог»), детали —
  `session-note.md` (2026-07-18 (4)).
- **Бэкенд-тесты**: 535/535 (`npm test` → vitest run; **НЕ** jest).
- **Прод-бэкенд = `origin/main`** (`/var/www/dacha-api`, деплой через `reset --hard`). Значит всё,
  что влито в `main`, на бэкенде уже в проде — пометки «не задеплоено» на старых пунктах ниже
  относятся к моменту их написания и не означают, что фичи нет на сервере. Отдельного деплоя
  требуют только веб-статика (`npm run build` → `/var/www/dacha-web`), лендинг и Android-релиз.
- **Лента «Мой участок»**: запись-центричная (`action`/`photo`/`milestone`), единый блок «действие+заметка+фото»
  на ленте и в журнале посадки. В проде (`GET /feed`, без миграции). Android — vc6/1.0.3 (не опубликован).
- **Автопостер ВК** (маркетинг): очередь с расписанием `vk_post_queue` (миграция 048) + cron `vkQueueJob`,
  публикует посты из md-файла контента в сообщество `calendacha`. Нужен ПОЛЬЗОВАТЕЛЬСКИЙ VK-токен. Ops — `docs/DEPLOY.md`.
- **Автопостер Telegram** (маркетинг, 2026-07-18): та же очередь `vk_post_queue` (миграция 058,
  колонки `telegram_*`) + cron `telegramQueueJob`, публикует в канал `@calendacha` через бота
  `@calendacha_bot` (Bot API). Независимый статус публикации от ВК. Ops — `docs/DEPLOY.md`.
- ✅ **E3 исправлен (2026-06-25)**: Android JVM unit-тесты теперь запускаются в этом окружении.
  Корень — Gradle-тест-воркер на Windows не грузил классы из `build/`-каталога с кириллическим путём
  (`ClassNotFoundException`, native-кодировка classpath-argfile, баг JVM/Gradle gradle/gradle#30304).
  Фикс — `android/build.gradle.kts`: при не-ASCII пути проекта `buildDir` всех модулей переносится в
  `%LOCALAPPDATA%\dacha-android-build` (ASCII), исходники остаются на месте. После фикса всплыл
  реальный баг в `TodayViewModelTest` — relaxed-мок `ActionsRepository` не синтезировал `SharedFlow<T>`
  (MockK `KotlinNothingValueException`), исправлено явным стабом в `setUp()`. `:app:testRustoreDebugUnitTest`
  и `:app:testGplayDebugUnitTest` — зелёные.
- **F1 офлайн «Сегодня»** (Android+backend): влит в `main` (read-кэш `TodayCache` + очередь записи
  `ActionQueue` + `client_id`-идемпотентность). **Backend задеплоен** (прод `8f0b09a`, миграция 045
  применена на проде). Android-часть — в сборке vc7/1.0.4, ждёт публикации в магазинах. См. session-note (2026-06-21).

## Монетизация (ФИНАЛ 2026-07-18 — free+premium, без триала)

Web + Google Play + RuStore — **free+premium, БЕЗ рекламы.** 1 сад и до `FREE_PLANTING_LIMIT`
(`backend/src/utils/access.js`, сейчас 3) активных посадок одновременно — **бесплатно навсегда**,
без ограничения по времени. Подписка «Дачник Про» снимает лимит на число посадок. Прежняя
7-дневная триал-модель (ФИНАЛ 2026-06-16) отменена: пользователи не успевали сформировать привычку
до paywall — гейт бил по основному циклу (посадка/уход/урожай) раньше, чем накапливалась история,
которую жалко терять.
**Гейт — двухчастный (обновлено 2026-08-06, коммит `0f12fa7`):**
1. `POST /plantings` → 402 `plan_limit_reached` сверх free-лимита (создание новой посадки).
2. **Запись по посадке сверх free-набора** → 402 `planting_locked`. Без платного доступа изменяемы
   только **первые `FREE_PLANTING_LIMIT` активных посадок по id**; остальные видны, но только для
   чтения. Закрыты: `PATCH /plantings/:id/{stage,info}`, `POST /actions`, `POST /harvests`,
   `POST /photos`, `POST /reminders`. Логика — `utils/access.js` (`freeTierState`, `isPlantingLocked`),
   правило живёт в одном SQL-фрагменте.
   **Намеренные исключения**: `stage='done'` не блокируется (архив — дописать урожай после сезона);
   перевод в `done` и `DELETE` разрешены всегда — «уменьшающие» операции освобождают слот, иначе
   выйти из лимита можно было бы только удалением данных.
   `GET /plantings` и `GET /plantings/:id` отдают `locked: boolean` — клиенты рисуют read-only
   заранее (бейдж/баннер, скрытые контролы, путь к оплате), а не ловят 402 по нажатию.

> **Зачем**: до 2026-08-06 гейт стоял только на создании, поэтому подписки на один месяц в начале
> сезона хватало, чтобы завести все посадки и вести сезон бесплатно. Автопродления нет ни в Google
> Play, ни в RuStore, так что дыра была рабочей, а не теоретической. Подробности — `backend/CONVENTIONS.md` §6.

⚠️ **Открытый хвост**: `GET /today` считает care-задачи по ВСЕМ активным посадкам, не глядя на
`locked` — неплательщик с 50 посадками получает напоминания по всем, просто не может отметить
выполнение. Закрывать ли — продуктовая развилка, не техническая.

`GET`-роуты (сегодня/погода/справочник/лунный календарь/лента) не гейтятся.
Samsung снят с публикации. Оплата — **ЮKassa напрямую** (Shop ID 1376599), **разовые платежи**
(рекуррент запрещён самозанятому → продление вручную); доступ продлевает вебхук `/billing/webhook`.
Чек — НПД (422-ФЗ): **авто-регистрируется в «Мой налог»** через `lknpd.nalog.ru` (сервис ЮKassa «Чеки
для самозанятых» прекращён 29.12.2025). Поток: webhook → `payments.npd_status` → cron `nalogJob` →
`addIncome` через RU-релей (`nalog-relay.php` на reg.ru, `NALOG_RELAY_URL`) → письмо с чеком. Детали —
`docs/DEPLOY.md` («Мой налог») и `session-note.md` (2026-06-19). ⚠️ `YOOKASSA_RECEIPT_MODE=off`.
✅ **E2E подтверждён реальным платежом 19.06.2026** (payment 7 → `registered`). По пути исправлены 2 бага:
релей терял токен ФНС (Apache режет `Authorization` → шлём токен в `X-Relay-Auth`) и `scp` затирал секрет
релея (теперь секрет в файле `.relay-secret`, не в гите). Секрет `NALOG_RELAY_SECRET` ротирован. Детали — session-note (2).
Серверный гейт: `utils/access.js` `hasAccess` (`subscription_until`/промо — доступ «Дачник Про»,
БЕЗ триала) + `FREE_PLANTING_LIMIT` + `isPlantingLocked` (см. выше). Рекламной модели
(Yandex Ads / РСЯ) больше нет — выпилена вместе с Samsung.

**Факт на 2026-08-06: платящих клиентов — 0.** В `payments` два `succeeded`, оба с собственного
тест-аккаунта `e-krukov@ya.ru` (`is_test=true`) — это E2E-проверки НПД-чека 09.06 и 19.06, не выручка.
Зарегистрировано 42 аккаунта, из них 20 тестовых → **22 реальных пользователя** (июнь 4 · июль 11 ·
август 7). Воронка регистрация→оплата пока 22 → 0. См. «Бизнес-статус» ниже.
⚠️ Код samsung-флейвора в Android ещё не удалён (не срочно, флейвор просто не публикуется).
⚠️ `docs/gtm-strategy-ru.md` писался под триал-модель — метрики нуждаются в пересмотре под free→paid
(флаг оставлен в начале файла). Письма жизненного цикла (`trialEmailsJob.js`) переработаны под
free-tier (2026-08-08): календарные дни 1/3/5/8/29/50 (хвост продлён — free бессрочный, обрывать
на дне 8 нет причины) + событийное письмо-оффер «Дачник Про» (бывший день 6), которое шлётся не по
календарю, а в момент, когда юзер реально упёрся в лимит посадок (`FREE_PLANTING_LIMIT`).

## Актуальные открытые задачи

**✅ F2: AI-диагностика болезней/вредителей по фото (РЕАЛИЗОВАН и ЗАДЕПЛОЕН 2026-08-09, backend
`8ca74b2`, веб `9962a68`, Android — код готов, релиз не собирался):**
Платная фича («Дачник Про» + 3 бесплатных проверки на аккаунт навсегда, без сброса): фото →
Qwen-VL (Alibaba DashScope, `qwen-vl-plus`) → closed-set диагноз (кандидаты — ТОЛЬКО болезни/
вредители сфотографированной культуры из своего `guide_entries`, не открытый вопрос).
Прототип на 15-20 фото показал 20% точности на open-set vs 65-94% на closed-set — архитектурное
решение задокументировано в памяти `project_dacha_ai_diagnosis.md`.
- Backend: миграция `066_photo_ai_diagnosis.sql` (`ai_diagnosis`/`ai_diagnosed_at` на
  `planting_photos`, аддитивно), `services/aiDiagnosisService.js` (env `AI_DIAGNOSIS_API_KEY`,
  без ключа — `isEnabled()=false`), `POST /photos/:id/diagnose` (гейт: 503 без ключа → 402 без
  платного доступа/бесплатного лимита → 404 IDOR → 422 если у культуры нет записей в
  справочнике). Тесты: `aiDiagnosisService.test.js` + `photos.test.js` (+29, всего 535).
  Число показываемых кандидатов подстраивается под размер справочника культуры (1→1, 2→1,
  3-4→2, 5+→3) — иначе на культурах с 1-2 записями «топ-3» превращался в пересказ всего
  справочника, выглядело фальшиво (найдено при живой проверке).
- Web: кнопка «Определить болезнь/вредителя» в дневнике фото ПЛЮС прямой вход с вкладок
  «Болезни»/«Вредители» (`DiagnosePhotoButton.tsx` — снял фото → сразу загрузка+диагноз без
  похода в дневник). Тот же диагноз доступен и из ленты «Мой участок» (`ProfileScreen.tsx` —
  там независимый второй просмотрщик фото, для него тоже правился отдельно).
- Android: `PhotosRepository.diagnosePhoto()`, `PlantingInfoViewModel` (`diagnosePhoto` — по
  существующему фото из дневника; `diagnoseNewPhoto` — быстрый вход с вкладок «Болезни»/
  «Вредители», зеркало web). Код готов и компилируется, в релиз ещё не собирался.
- Живые баги, найденные при ручной проверке (все пофикшены до этой строки):
  просмотр фото открывался в натуральную величину без скролла (веб, флексбокс без `min-h-0`);
  кнопка пряталась под системной навигацией (Android, `Dialog` не давал системным инсетам
  дойти до композиции — фикс `WindowCompat.setDecorFitsSystemWindows`).
- **Осознанно НЕ сделано:** расширение самого справочника болезней/вредителей для культур с
  тонким покрытием (1-2 записи) — отдельная содержательная задача, не блокирует фичу.

**✅ vc11/1.0.8 ОПУБЛИКОВАН в Google Play и RuStore (модерация пройдена, подтверждено 2026-08-03).**
Google Play — первый production-релиз после closed testing. Что вошло: тёмная тема, «Мои семена»,
советы дня по культуре, фиксы шторок и тёмной темы, просмотр фото пакетика, ссылка на веб-версию.
Тексты карточек и «Что нового» — `docs/aso-rustore.md` + `session-note.md` (2026-07-30 (2)).

**✅ vc12/1.0.9 ОПУБЛИКОВАН 2026-08-10** (`session-note.md`, 2026-08-09 (2)): ИИ-диагноз по фото,
фото 55 культур в справочнике, погода в задачах дня, подсказки дневника + «Заметки», плюс R8/WebP/
edge-to-edge тех-фиксы (versionCode 11→12).

**Актуальная версия в сторах — vc14/1.0.11** (versionCode 13→14, versionName 1.0.10→1.0.11,
`session-note.md`, 2026-08-12): фикс краша карточки многолетника (смородина/крыжовник/деревья) +
сезонность ухода теперь привязана к календарю/факт. погоде по региону, а не к дате посадки
(миграции 067-069, на проде). Промежуточная versionCode 13/1.0.10 отдельно не публиковалась —
вошла в vc14 без отдельного релиза в сторы.

**Правки on-device QA, вошедшие в vc11** (2026-07-30, `020c600` → `933e1a4`):
1. Шторки: `ModalBottomSheet` без `skipPartiallyExpanded` открывается на половину экрана и
   обрезает нижние поля/кнопку «Сохранить», без `verticalScroll` до них не добраться из-под
   клавиатуры. Чинены `SeedsScreen` и `AddHarvestSheet` («Убрать урожай» — дефект был и в проде).
2. Белая плашка в тёмной теме (`SeedsScreen`) — следствие устаревшего правила в
   `android/CONVENTIONS.md` («явно White!», писалось до тёмной темы). Правило переписано.
3. Карточка «Подтвердите email» (`AccountSection`) — тот же дефект, найден аудитом.
4. Тап по фото пакетика открывает его на весь экран (`ui/common/FullScreenPhotoDialog.kt`);
   остальная карточка по-прежнему ведёт в редактирование.
5. Карточка «Веб-версия» в табе «Ещё» — ссылки на веб в приложении не было вовсе, хотя
   в карточке магазина она теперь заявлена как преимущество.

Оба каркаса (шторка, карточка) зафиксированы в `android/CONVENTIONS.md`.

**После публикации:**
- ✅ RuStore Console: обновлённый FAQ карточки перенесён, скриншоты залиты (2026-08-06).
- ✅ Google Play: скриншоты обновлены (2026-08-06).
- ⬜ **Проверить на живом релизе регистрацию → оплату → чек НПД.** Последний E2E — 19.06, почти
  два месяца назад; с тех пор менялся гейт доступа. Единственный незакрытый релизный хвост.

**✅ Лендинг: ссылки на Google Play (2026-07-30, ЗАДЕПЛОЕНО `1dc48b2`).** Заглушка «Скоро»
заменена рабочей кнопкой в блоке загрузки и ссылкой в футере
(`play.google.com/store/apps/details?id=ru.dachakalend.app`); убраны мёртвые стили заглушки.
Тем же деплоем выехала правка «45+ культур» → «55 культур» в 7 местах, включая `meta
description`, og/twitter и FAQ-схему. Проверено на живом сайте: `/` → 200, 2 ссылки на Play,
7 упоминаний «55 культур», остатков заглушки 0.

**✅ Веб: CSP рубила все приватные фото (2026-07-30, ЗАДЕПЛОЕНО `54d84df`).** `img-src` не содержал
`blob:`, а `AuthImage` тянет приватные фото (X-Accel-Redirect по Bearer) через
`fetch` + `createObjectURL` — молча ломались **все** фото в вебе: лента, фотодневник, карточки
действий, семена. Баг виден только в проде: vite-плагин `strip-csp-meta-in-dev` убирает мету в dev.
Заодно: `wss://mc.yandex.*` в `connect-src` (вебвизор Метрики), правка пакетика целиком
(культура/сорт/срок — бэкенд это умел, UI не давал) и просмотр фото по клику.
Лендинг с «55 культур» выложен тем же заходом.

**✅ Инвентарь семян (РЕАЛИЗОВАН 2026-07-29, ЗАДЕПЛОЕН — проверено 2026-08-06: таблица `seeds`
на проде, `GET /seeds` → 200, вошёл в vc11/1.0.8):**
«Мои семена» — коробка с пакетиками: культура, сорт, фото пакетика, срок годности с проверкой.
Просроченные поднимаются вверх списка и считаются в шапке; «истекает в этом сезоне» — отдельным
цветом. Флаги `expired`/`expires_this_year` считает SQL, клиенты не дублируют арифметику.
- Backend: миграция `060_seeds.sql` (таблица + GRANT), роут `routes/seeds.js`
  (GET/POST/PATCH/DELETE + `POST /seeds/:id/photo` multipart + приватная отдача через
  X-Accel-Redirect). Срок с пакетика принимается месяцем (`2027-12` → `2027-12-31`).
  Тесты: `seeds.test.js` (+18, всего 455).
- Web: `screens/SeedsScreen.tsx`, маршрут `/seeds`, пункт «Мои семена» в меню «Ещё».
- Android: `SeedsRepository` + `ui/seeds/{SeedsScreen,SeedsViewModel}.kt`, `Screen.Seeds`,
  карточка в табе «Ещё». Оба флейвора компилируются, unit-тесты зелёные.
- **Осознанно НЕ сделано** (см. `docs/ux-roadmap.md`): OCR пакетика, список покупок,
  количество пакетиков, отметка «посеяно».
- **Деплой**: выполнен (миграция 060 применена, веб пересобран).

**✅ Советы дня стали культуро-специфичными (2026-07-30, ЗАДЕПЛОЕНО `af7db8f`):** совет
подписывается именем культуры, но брался только из общего по стадии `STAGE_TIPS` — поэтому
«окучьте картофель» попадало на горох, а «трескаются корнеплоды» на укроп. Подключён слой
`getCropStageTip` из **уже имевшейся** агрономии в `crops` (`watering_details.notes`,
`fertilizing_schedule` по стадиям, `diseases`/`pests`; роут их SELECT'ил и выбрасывал).
Приоритет: совет про культуру → фолбэк на общий пул. Пул вычищен от культуро-специфичных
формулировок + тест-страж. Тесты 461/461. Проверено на живом проде.
Заодно (миграция **061**): агрономия ~24 культур жила **только в проде, мимо git** — прод-значения
зафиксированы в миграцию, дописана Петуния. Сейчас **55/55** культур с полными данными.
Детали — `session-note.md` (2026-07-30).

**✅ Фикс favicon на лендинге (2026-07-14, ЗАДЕПЛОЕНО прямым `scp` + nginx location):**
Яндекс.Вебмастер не видел иконку — она была data-URI SVG-эмодзи, `/favicon.ico` отдавал 404. Сделан
реальный `favicon.ico`/`favicon.svg`/`favicon-192.png`/`apple-touch-icon.png`, подключены на всех
страницах лендинга, добавлен nginx location для этих файлов (иначе уходили в proxy_pass на API).
Детали — `session-note.md` (2026-07-14). ✅ **В git всё есть** — `favicon.ico`, `favicon.svg`,
`favicon-192.png`, `apple-touch-icon.png` закоммичены тем же днём (`6828dfa`, 2026-07-14);
прежняя пометка «не закоммичено» устарела и снята при проверке 2026-07-30.

**✅ Фикс «пуши по собранным посадкам» (2026-06-29, ЗАДЕПЛОЕНО `8f0b09a`):**
Жалоба «по завершённым посадкам идут пуши» оказалась не багом фильтра `stage='done'` (он работает), а следствием неидемпотентного `POST /gardens`: эндпоинт плодил участки-дубли (комментарий обещал «вернуть существующий», код создавал новый при < 3). Забытый участок-сирота с активной посадкой слал пуши, а дайджест показывает только культуру → выглядело как пуш по завершённой. Фикс: `POST /gardens` идемпотентен (возвращает первый существующий, 200, без INSERT); удалён мёртвый лимит-409. Прод-данные: осиротевшая посадка завершена. Детали — `session-note.md` (2026-06-29 (2)).

**✅ Унификация «Убрать урожай» (РЕАЛИЗОВАНО + ЗАДЕПЛОЕНО 2026-06-30; backend `8f0b09a` + веб пересобран `/var/www/dacha-web`):**
Карточка «Убрать урожай» на «Сегодня» (Android + веб) теперь открывает специализированный лог урожая с преднастроенной посадкой и чекбоксом «Это весь урожай в сезоне» (завершает посадку переводом в `stage=done`). Backend: cooldown 3 дня — после записи в `harvests` карточка не показывается снова сразу. Веб: паритетная кнопка «Завершить сезон» на странице посадки. Деталь — `session-note.md` (2026-06-29). Миграций нет.

**✅ Паритет веб-версии с Android — раздел «Профиль» + журнал (P1+P2, ЗАДЕПЛОЕНО 2026-06-23 `e5913fe`):**
Веб догнал Android по разделу «Профиль / Мой участок» (табы Лента/Статистика/Справочник, лента
`action/photo/milestone` с группировкой по месяцам, бесконечной подгрузкой и просмотром/управлением фото)
и журналу (фильтр по культуре + удаление записи). В навбаре Справочник→Профиль. Бэкенд `/feed` уже был в
проде — деплой только веба (`npm run build` → `/var/www/dacha-web`). План и статус — `docs/web-parity-plan.md`
(осталось: P4 онбординг-выбор культур, P5 snooze/reverse-geocode/Web Push).

**✅ Персональная лента «Мой участок» (РЕАЛИЗОВАНА `baa79e9` 2026-06-22, В ПРОДЕ — `GET /feed` → 200,
проверено 2026-08-06):**
Вкладка «Инфо» заменена на «Профиль / Мой участок» (табы Лента / Статистика / Справочник). Сводная
приватная лента всех фото + вехи сезона (посев/высадка/первый урожай/завершение), группировка по месяцам.
Backend `GET /feed` (UNION, пагинация, без миграции). Фундамент под будущую социализацию. Спека:
`docs/superpowers/specs/2026-06-22-personal-feed-design.md`. Заодно багфиксы тестера: переводы действий
в «Сделано сегодня», клик по карточке посадки, фотолента по месяцам на экране посадки (`5aa76ff`).
Деплой: `git push origin main` → VPS `fetch && reset --hard origin/main` → `pm2 restart dacha-api` (миграции нет).
**Отложено (v1):** счётчики в шапке профиля; инлайн-вкладки Статистика/Справочник; веха «Цветение» (нужна отметка стадии).


**✅ Групповые care-задачи — мульти-посадочное действие (РЕАЛИЗОВАНО + ЗАДЕПЛОЕНО 2026-06-17):**
Сгруппированная care-задача («Прополка: Капуста пекинская, Редис») стала кликабельной → лист
действия с мульти-посадочным режимом. Заголовок — список культур с крестиком удаления (мин. 1),
одно действие пишется во все оставшиеся посадки (клиент циклит `POST /actions` по каждой —
сохраняется per-planting IDOR-проверка; отдельный batch-эндпоинт не нужен).
- Backend: `buildTasks` добавляет `planting_ids: number[]` + `crop_names_with_ids: {id,name}[]`
  в групповой `care_task_due`; `formatTasks` пробрасывает их (одиночные → null). Тесты **266** (+2).
- Web: `TodayTask` += поля; `ActionLogSheet` принимает `plantings: CropRef[]` + `title`
  (групповой режим со списком и крестиками); групповая карточка кликабельна. Проверено в превью.
- Android: `Models.kt` += `CropRef` и поля; новый `MultiActionLogBottomSheet` (общий `ActionLogSheetImpl`);
  VM `logActionMulti`/`logTransplantingMulti`; `TodayScreen` открывает мульти-лист для групповых.
  `:app:compileGplayDebugKotlin` BUILD SUCCESSFUL.
- **✅ Задеплоено**: backend на VPS (`git reset --hard origin/main` → `232d024`, `pm2 restart`,
  health ok; прод `/today` отдаёт `planting_ids`+`crop_names_with_ids`), веб пересобран
  (`/app/` → 200). Android AAB/APK публикует пользователь.

**✅ Задеплоено 2026-06-16** (`b8641ef` контактная почта + `9ffa82d` шапка лендинга): VPS на `52dfb5d`,
лендинг и веб пересобраны/выложены, `/` и `/app/` отдают 200, на сайте новый email `dacha@studio1008.com`.
Ящик `dacha@studio1008.com` создан 2026-06-16 — задача закрыта полностью.

**География распространения — только Россия** (решено 2026-07-30, оба магазина). Не расширять
без подготовки: геокодер отдаёт подсказки городов только по РФ (`countrycode=ru`), климатическая
зона вне справочника регионов РФ падает в дефолт 4 (средняя полоса) — сроки посева для юга СНГ
и диаспор будут врать, а оплата через ЮKassa вне РФ не проходит. Ближайшие осмысленные кандидаты
на будущее — Беларусь и Казахстан (дачная культура та же, зона близка к дефолтной, карты «Мир»),
и им потребуется правка геокодера.

**Google Play (при публикации):**
- ✅ **Третий SHA-1** (App signing key certificate) получен и встроен в ограничение Firebase API-key — FCM на Play-сборке регистрируется. Debug+release SHA-1 тоже добавлены.
- Заменить кнопки магазинов «Скоро» (`#download` `.store-btn[aria-disabled]`) и store-ссылки футера на реальные URL.
- Гайд: `docs/gplay-publishing-guide.md`. ASO-карточки: `docs/aso-gplay-samsung.md` (актуальна, платная модель), `docs/aso-rustore.md`.
- **Название Play** (лимит 30): «Календарь дачника: сад, огород». **Категория**: «Дом и сад» / «Книги и справочники» (Lifestyle в списке нет). Package `ru.dachakalend.app`.
- **Легал-URL (live):** политика `…/privacy`, оферта `…/offer`, удаление `…/account-deletion` (для Data safety → Account deletion).
- **Data Safety** выверен (см. session-note (4)): Email, User IDs, История покупок, Approx+Precise location, Other UGC, Device ID — collected/not shared; рекламы/Advertising ID нет.
- **Feature graphic** 1024×500 — `docs/store-assets/feature-graphic.png`.
- ⚠️ Личный аккаунт → обязательно closed testing (12 тестировщиков, 14 дней) перед production.

**Демо-данные / тест-аккаунты:**
- `demo@dacha.ru` / `demo1234` (прод, store=gplay) — участок Краснодар (garden 12) с посадками:
  на «Сегодня» по одной задаче каждого типа (для скриншотов). См. session-note (4).
  ⚠️ Триала у аккаунта больше нет (модель отменена 2026-07-18) — это обычный free-пользователь,
  и на нём видно поведение `planting_locked`: 5 активных посадок, первые 3 по id рабочие,
  остальные read-only. Учитывать при съёмке скриншотов.
- Скиллы статистики (локальные): `/statistic`, `/statistic_user`.

**Пуши (FCM):**
- ✅ Гигиена доставки (2026-06-17): мёртвые токены (`registration-token-not-registered`) удаляются из
  `push_tokens`; `care_alert_log` помечается только при фактической доставке (см. session-note).
- ✅ FCM re-register на тест-устройстве — токен живой (improvement-plan 1.3).
- ✅ **Третий SHA-1** (App signing key) получен и встроен в ограничение Firebase API-key — FCM на
  Play-сборке регистрируется.

**P4 «Аккаунт и безопасность» — хвост (отдельные циклы):**
- ✅ C — смена города/региона (Android `GardenEditScreen`+`CityInputField`, web `EditGardenScreen`).
- D — управление участками + переключатель активного сада. ⚠️ 2026-06-25: владелец сомневается в
  востребованности — сначала оценить реальный спрос, не строить вслепую.
- Уведомления на web (Web Push / настройки типов).

**✅ Грядки + севооборот («план участка») — ГОТОВО (2026-07-01):** бэкенд (миграции 052/053 + GRANT 055)
+ UI на web (в проде) и Android (в `main`, ждёт релиза): поле «Место», инлайн CRUD грядок (грунт/теплица),
подсказка севооборота по семейству за 3 года, правка «Места»/«Условий» в карточке. «Условия» перенесены
из формы создания в карточку; на вебе добавлен выбор способа посадки. Планы:
`docs/superpowers/plans/2026-06-30-garden-beds-{web,android}-ui.md`. Хвост: Android-релиз + on-device QA.

**✅ Лунный календарь (РЕАЛИЗОВАН + ЗАДЕПЛОЕН 2026-07-01, backend+web; Android в `main`):** встроен
в существующий экран «Календарь» (не отдельным экраном — по фидбэку владельца): иконка фазы Луны
в каждой ячейке дня + карточка «фаза+совет+"Не сажать"» в панели выбранного дня, рядом с обычными
задачами. `GET /moon-calendar?year=&month=` (прод, health ok), веб пересобран (`/app/` 200). Тесты
398/398 (+5). Хвост: on-device QA. Детали — `session-note.md` (2026-07-01 (2) и (3)).

**✅ Опубликовано:** Google Play и RuStore оба на **vc14/1.0.11** (versionCode 13→14, versionName
1.0.10→1.0.11) — фикс краша карточки многолетника (смородина/крыжовник/деревья) и сезонность
ухода, привязанная к календарю/факт. погоде по региону вместо даты посадки (миграции 067-069).
Текст «Что нового» — `session-note.md` (2026-08-12). Промежуточная versionCode 13/1.0.10 отдельно
в сторы не выкатывалась.

**vc12/1.0.9** (опубликован 2026-08-10) собирал: **ИИ-диагноз болезней/вредителей по фото**
(3 бесплатные проверки на аккаунт), фото у 55 культур в справочнике, погода в задачах дня
(дождь/жара/заморозки) с приоритетом по срочности, дневник наблюдений с подсказками
(«Другое»→«Заметки» как отдельная фича свободной записи), плюс тех-фиксы для рекомендаций
Google Play Console: R8 `shrinkResources`, растровый `ic_sunflower_png` → WebP (203 КБ→20 КБ),
обновление `androidx.activity`/`core-ktx` под edge-to-edge на `compileSdk 36`. Текст «Что нового» —
`session-note.md` (2026-08-09 (2)).

**⚠️ Google Play Console: рекомендации edge-to-edge/растровые изображения продолжают висеть после
фикса выше — разобрано и закрыто как неактуабельное (2026-08-14).** Три пункта:
`setStatusBarColor`/`setNavigationBarColor`/`LAYOUT_IN_DISPLAY_CUTOUT_MODE_SHORT_EDGES` (классы
`b21.b`/`d21.b`/`f21.b`/`u2.p`) и декодирование битмапов без библиотеки загрузки (класс `dm0.t`) —
код не наш: в проекте нет прямых вызовов этих API (`MainActivity.kt` уже вызывает официальный
`enableEdgeToEdge()`), Coil уже используется везде (`coil-compose`). По докам AndroidX,
`enableEdgeToEdge()` сам внутри себя вызывает `setStatusBarColor`/`setNavigationBarColor` как
compat-шим для старых API — `activity-compose 1.13.0` уже последняя стабильная версия, апгрейдить
некуда. Битмап-декодер — встроенный в `firebase-messaging` загрузчик картинки для push с полем
`image`; бэкенд (`fcmService.js`) это поле не передаёт, но класс всё равно лежит в SDK мёртвым
грузом. Проверил доступные апдейты (`firebase-bom` 33.16.0→34.17.0, `firebase-messaging`
24.1.0→25.1.1, `rustore pushclient` 6.0.0→8.0.0-rc01) — ни в одном changelog нет фикса именно
этих warning'ов, а `firebase-bom 34.0.0` вдобавок breaking (убраны KTX-модули, устарели
`getToken`/`deleteToken`/`onNewToken`). **Решение: не трогать зависимости и код ради этих
warning'ов** — они не блокируют публикацию (это рекомендации, не ошибки) и не уйдут, пока Google
сам не обновит внутренности AndroidX/Firebase SDK.

**vc11/1.0.8** собирал: тёмная тема (тумблер «Система/Светлая/Тёмная») + контраст карточек,
**инвентарь «Мои семена»**, советы дня по культуре, фикс ISO-дат на «Урожае», фикс подсказки
севооборота, фиксы шторок, просмотр фото пакетика, ссылка на веб-версию. Для Google Play это был
первый production-релиз после closed testing (опубликован 2026-08-03).

**✅ SEO-справочник `/spravochnik/` — ГОТОВО И В ПРОДЕ (2026-07-02):** статические страницы культур
(`/spravochnik/kultury/{slug}/`, 55 шт.) и проблем растений (`/spravochnik/problemy/{slug}/`, 78 шт.)
на базе публичных `/crops`+`/guide`, генератор `backend/scripts/generate-spravochnik.js` (запускать
после `backfill-crop-slugs.js`). Title/description проверены и уложены в SEO-лимиты (60/160 симв.)
реальным аудитом живых страниц, не только по дизайну. Ссылка «Справочник» в nav/footer лендинга.
Файлы верификации Google Search Console + Яндекс.Вебмастер выложены в корень сайта — владельцу
осталось подтвердить права в обоих сервисах и добавить sitemap.xml (140 URL) вручную. Детали,
включая побочно найденный и исправленный баг с правами на таблицах Postgres (`care_alert_log`,
`garden_beds`, `subscription_emails` принадлежали `postgres` вместо `dacha_user`) — `session-note.md`
(2026-07-02). Деплой — `docs/DEPLOY.md` («Справочник /spravochnik/»).

**IndexNow подключён (2026-07-02):** `backend/scripts/submit-indexnow.js` шлёт все URL из
`sitemap.xml` на `https://yandex.com/indexnow` (по протоколу уведомление уходит и другим
участникам, не только Яндексу). Требует `INDEXNOW_KEY` в `.env` + файл-подтверждение `{ключ}.txt`
в корне сайта. Первая отправка успешна (202, 140 URL) — гонять после будущих партий новых страниц.

## Бизнес-статус (сверено с прод-БД 2026-08-06)

**Платящих клиентов — 0.** Цель кампании (`docs/promo-plan-first-paying.md`) — ≥10 платящих к
~22.08.2026 (3 месяца с запуска 22.06). До контрольной точки две недели.

| Метрика | Факт |
|---|---|
| Реальных пользователей | 22 (всего 42 аккаунта − 20 тестовых) |
| Регистрации по месяцам | июнь 4 · июль 11 · август 7 |
| Платящих клиентов | **0** |
| `payments.status='succeeded'` | 2, оба с `e-krukov@ya.ru` (`is_test=true`) — E2E НПД 09.06 и 19.06 |
| Активных подписок сейчас | 1 (тот же тест-аккаунт) |

Продукт работает и растёт по регистрациям, но конверсия в оплату нулевая на всём горизонте
кампании. Это не «мало данных» — это 22 регистрации без единой оплаты. К 22.08 нужно решение,
что менять: канал, оффер, момент показа paywall или сам free-лимит. Гипотезы не проверялись —
воронка по шагам (регистрация → участок → посадка → упор в лимит → открытие paywall) не
инструментирована, поэтому **сейчас неизвестно, где именно отваливаются**.

⚠️ Read-only гейт (2026-08-06) впервые закрывает бесплатное ведение сезона сверх 3 посадок —
это меняет условия воронки, и метрики до/после сравнивать напрямую нельзя.

**⚠️ Полуготовые фичи (бэкенд без UI / в процессе):**
- **Единое снуз/удаление задач дня** (`unified-task-dismiss`): миграция `054_today_task_dismissals.sql`
  создана; роуты (`POST /today/tasks/dismiss`, `GET /today/tasks/dismissed`) + фильтрация в `GET /today`
  + клиенты — не реализованы (перепроверено 2026-08-06). План/спека:
  `docs/superpowers/{plans,specs}/2026-06-27-unified-task-dismiss*`.

**Технический долг:** ✅ закрыт (2026-06-30).
- ✅ **E3** — исправлено 2026-06-25, см. «Текущий статус» выше.
- ✅ **2.1** `logout()` зовёт `deletePushToken()` до очистки auth — было уже реализовано (план был стале).
- ✅ **2.2** код samsung-флейвора + весь рекламный стек (РСЯ) удалён; остались rustore+gplay, обе компилируются.

**«Could» из ТЗ:**
- §5.8 сравнение урожая по сезонам · §5.2 профиль участка (отдельный экран) · §5.1 поля участка (площадь, тип почвы).

**✅ Фото культур (2026-08-06, ЗАДЕПЛОЕНО `65dfbae`, миграция 064):** 55/55 культур получили
фото — `web/public/media/crops/<slug>.jpg` (900 px, 8.7 MB, в репо, выезжают со сборкой веба),
источник Wikimedia Commons, только свободные лицензии, атрибуция в `crops.image_credit`.
Инфраструктура показа была готова с миграции 047, но файлов не было — карточки культур стояли
пустыми. Подбор воспроизводим: `node backend/scripts/fetch-crop-images.js`; источники по каждому
кадру — `backend/scripts/crops-images.sources.json`. Проверено на проде: `/crops` отдаёт
`image_url` у 55/55, файлы отдаются как `image/jpeg`.

**✅ Хвост фото справочника (2026-08-06, ЗАДЕПЛОЕНО `44b0d9c`, миграция 065):** 76 из 78 записей
с фото (было 73). Тем же скриптом в режиме `--guide` — источники в
`backend/scripts/guide-images.sources.json`. Кадры сверены с привязкой записи к культурам
(`crop_guide_entries`), чтобы болезнь показывалась на той же культуре. Осталось 2 записи
(`fomoz-suhaya-gnil`, `lukovyy-skrytnohobotnik`) — на Wikimedia Commons нет подходящих кадров,
нужны свои снимки; неверное фото болезни хуже отсутствующего.

**✅ Градация срочности задач «Сегодня» (2026-08-08, ЗАДЕПЛОЕНО `954e74b`):** раньше все
просроченные задачи красились одинаково — на демо-данных 6+ красных пилюль одновременно, сигнал
терялся. Ступень считает бэкенд (`urgencyLevel` в `utils/todayLogic.js`), поле `urgency` в каждой
задаче `/today`: `critical` (заморозки) · `late` (просрочка > 7 дн., красный) · `soon` (1–7 дн.,
приглушённый) · `normal`. Клиенты (web/Android) только красят. Порог сначала стоял 3 дня —
на живых прод-данных в `late` попадало 6 задач из 7 (просрочки там 3–21 день), экран оставался
таким же красным; после проверки поднят до недели, распределение стало честным (3 late / 4 soon).

**Этап 2+ (полный бэклог):** `docs/ux-roadmap.md`. Сверено с кодом и продом 2026-08-06 —
из крупного НЕ сделано:
- **Единое снуз/удаление задач дня** (`unified-task-dismiss`) — есть только миграция 054, роутов и
  клиентов нет. Снуз до сих пор локальный (`TokenStorage`), между устройствами не переезжает.
- **Управление участками + переключатель активного сада** (⏸ спрос под вопросом) — в
  `web/src/garden/GardenContext.tsx` есть `gardens` и `active`, но сеттера нет; UI переключения нет нигде.
- **Настройки уведомлений на web** — в `web/src/screens/SettingsScreen.tsx` нет ни одного упоминания
  пушей (на Android тумблеры типов есть).
- **§5.8 сравнение урожая по сезонам** — в `routes/analytics.js` нет ни группировки по годам, ни `EXTRACT`.
- **§5.1 поля участка**: `soil_type` бэкенд принимает в `POST/PUT /gardens`, но UI нет ни на одном
  клиенте; `area_m2` есть колонкой в `001_init.sql` и не используется даже роутом.
- **П8 десктоп-layout веба**; фильтр журнала по типу и редактирование записи.

Закрыто (пометки в старых редакциях устарели): грядки/севооборот UI ✅ (2026-07-01),
P5 изображения культур ✅ (55/55, миграция 064), хвост фото справочника — **76 из 78**
(миграция 065). Оставшиеся 2 записи (`fomoz-suhaya-gnil`, `lukovyy-skrytnohobotnik`)
требуют своих снимков: на Wikimedia Commons подходящих кадров нет, а «похожее» фото болезни
ставить нельзя — оно ведёт к ложному диагнозу.
Фото-диагностика — ❌ отклонена (низкая точность).

## Реализованные API (справка)

```
POST /auth/register  POST /auth/login  GET /auth/me  POST /auth/subscription
POST /auth/verify-email  POST /auth/resend-verification
POST /auth/forgot-password  POST /auth/reset-password
PATCH /auth/password  POST /auth/change-email  POST /auth/confirm-email-change  DELETE /auth/me
POST /promo/redeem
POST /billing/create-payment  POST /billing/webhook  POST /billing/cancel-autorenew
GET /guide  GET /guide/:slug
POST /gardens  GET /gardens  GET /gardens/:id  PUT /gardens/:id
GET /crops  GET /crops/:id  POST /crops  PUT /crops/:id
POST /plantings  GET /plantings  GET /plantings/:id
  PATCH /plantings/:id/stage  PATCH /plantings/:id/info  DELETE /plantings/:id
POST /actions  GET /actions  GET /actions/export
GET /weather?garden_id=
GET /recommendations?garden_id=
GET /today?garden_id=
POST /reminders  GET /reminders
GET /harvests  POST /harvests
GET /analytics/summary
GET /geocode/suggest?q=
POST /push-tokens  DELETE /push-tokens
```
