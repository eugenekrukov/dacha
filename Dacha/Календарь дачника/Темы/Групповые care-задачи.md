---
tags: [dacha, тема, фича]
---

# Тема: Групповые care-задачи (мульти-посадочное действие)

> обновлено: 2026-06-28, commit `1dc79dd`

Реализовано и задеплоено на всех трёх платформах (2026-06-17).

## Суть
Сгруппированная care-задача («Прополка: Капуста пекинская, Редис») стала кликабельной → лист действия с мульти-посадочным режимом. Заголовок — список культур с крестиком удаления (мин. 1), одно действие пишется во все оставшиеся посадки.

## Backend
`buildTasks` добавляет `planting_ids: number[]` + `crop_names_with_ids: {id,name}[]` в групповой `care_task_due`; `formatTasks` пробрасывает их (одиночные → null). Клиент циклит `POST /actions` по каждой посадке — per-planting IDOR-проверка сохраняется, отдельный batch-эндпоинт не нужен. Тесты: 266 (+2). См. [[02 Backend]].

## Web
`TodayTask` += поля; `ActionLogSheet` принимает `plantings: CropRef[]` + `title` (групповой режим со списком и крестиками); групповая карточка кликабельна.

## Android
`Models.kt` += `CropRef` и поля; новый `MultiActionLogBottomSheet` (общий `ActionLogSheetImpl`); VM `logActionMulti`/`logTransplantingMulti`; `TodayScreen` открывает мульти-лист для групповых.

## Деплой
Backend на VPS (`232d024`), веб пересобран, Android AAB/APK публикует пользователь.

## Связано
[[08 Статус и бэклог]] · [[Темы/Лента Мой участок]]
