# План доработок (приоритизированный)

> Составлен 2026-06-19. **Сверен с кодом и прод-БД 2026-08-06** — часть пунктов была помечена
> открытой, хотя давно закрыта (3.5 грядки-UI, 2.1), и наоборот: «~16 записей справочника» на деле 5.
> Порядок намеренный: **сначала техдолг и исправления, потом новые фичи.**
> Это исполнительный план; широкий бэклог этапа 2+ — `docs/ux-roadmap.md`. Статус задач —
> `summary.md`, история — `session-note.md`.
>
> Обозначения: `[x]` сделано · `[~]` частично · `[ ]` не начато · `⏸` отложено (внешний блокер).
>
> ⚠️ **Главное на 2026-08-06 — не в этом файле.** Продуктовый бэклог здоров, а бизнес-цель нет:
> 22 реальных пользователя, **0 платящих**, контрольная точка 22.08. Прежде чем брать любую задачу
> отсюда, стоит ответить, приблизит ли она первую оплату. См. «Бизнес-статус» в `summary.md`.

## Фаза 1 — Релиз-блокеры и операционные хвосты

- [x] **1.1 Авто-чек НПД работает end-to-end.** Подтверждено реальным платежом 19.06.2026
  (payment 7 → `registered`). По пути исправлены 2 бага (релей терял токен → `X-Relay-Auth`;
  `scp` затирал секрет → файл `.relay-secret`); секрет `NALOG_RELAY_SECRET` ротирован. См. session-note (2).
- [x] **1.2 Google Play: третий SHA-1** (App signing key) — получен и встроен в ограничение
  Firebase API-key. FCM на Play-сборке регистрируется.
- [x] **1.3 FCM re-register** — на устройстве живой fcm-токен (142 симв.).
- [x] **1.4 Пароль тест-аккаунта** — снято с плана (не важно).
- [x] **1.5 RuStore-ссылка на лендинге** — кнопки ведут на каталог приложения, задеплоено в прод.

## Фаза 2 — Технический долг (код)

- [x] **2.1 `logout()` чистит push-токен на клиенте.** Уже реализовано: `SettingsViewModel.logout()`
  зовёт `api.deletePushToken(...)` ДО очистки auth-токена (с обоснованием в комментарии). План был стале.
- [x] **2.2 Код samsung-флейвора удалён** (2026-06-30). Вырезан весь рекламный стек (РСЯ/Yandex Mobile
  Ads был только в samsung): productFlavor `samsung`, `src/samsung/`, no-op `Ads.kt` в gplay/rustore,
  `Ads.init/Banner/onContentEvent` в App/MainActivity, buildConfig `ADS_ENABLED/BANNER_AD/INTERSTITIAL`,
  зависимость `yandex.mobileads`. Остались флейворы rustore + gplay. Компиляция обоих — BUILD SUCCESSFUL.
- [x] **2.3 E3 — Android unit-тесты не запускаются.** Исправлено 2026-06-25: причина — Gradle
  тест-воркер на Windows не грузил классы из кириллического `build/`-пути; фикс — редирект `buildDir`
  в `android/build.gradle.kts` на ASCII-путь под не-ASCII проектом. Заодно поправлен всплывший
  реальный баг мока в `TodayViewModelTest`.

## Фаза 3 — Доработка существующего (хвост П4, недостающий UI)

- [x] **3.1 Смена города/региона** — РЕАЛИЗОВАНО. Android `GardenEditScreen` + `CityInputField`;
  web `EditGardenScreen`. Backend `PUT /gardens/:id` был готов.
- [ ] **3.2 Управление участками + переключатель активного сада.** Затрагивает «активный сад» по
  всему приложению — самая объёмная из тройки.
- [ ] **3.3 Настройки уведомлений на web** (на Android тумблеры типов уже есть; опц. время пушей).
- [~] **3.4 Android: эмодзи → иконки** — основные экраны переведены (UI-10, сессия 2026-06-21);
  остаётся хвост на вторичных экранах. Косметика/консистентность.
- [x] **3.5 Грядки + севооборот («план участка») — UI.** СДЕЛАНО 2026-07-01: поле «Место», инлайн
  CRUD грядок (грунт/теплица), подсказка севооборота по семейству за 3 года. Web в проде, Android
  вошёл в релиз. Пометка «клиентского UI нет» держалась в плане месяц после факта.

## Фаза 4 — Новые фичи (этап 2)

- [x] **4.1 П5. Изображения культур** — СДЕЛАНО ПОЛНОСТЬЮ. Инфраструктура — миграция 047
  (`crops.image_url`, показ в карточках/деталях, lazy-load, кредиты); **сами фото залиты только
  2026-08-06** (миграция 064, 55/55, Wikimedia Commons). До этой даты пункт два месяца стоял
  как `[x]`, хотя `image_url` был пуст у всех культур — отметка «залиты» была неверной.
- [~] **4.2 П5. Долгий хвост справочника** — **76 из 78** (2026-08-06, миграция 065; прежняя
  оценка «~16 записей» была неверной, их было 5). Оставшиеся 2 (`fomoz-suhaya-gnil`,
  `lukovyy-skrytnohobotnik`) с Wikimedia Commons не закрываются — нужны свои снимки либо
  iNaturalist/GBIF. Ставить «похожее» фото болезни нельзя: ведёт к ложному диагнозу.
- [x] **4.3 П7. Поиск культур** — Android (`CropsScreen` поиск + чипы) и web (`AddPlantingForm` комбобокс) готовы.
- [—] **4.4 П5. Фото-диагностика болезней** — ОТКЛОНЕНО (низкая точность распознавания). Ни Claude Vision, ни Kindwise не дают приемлемого качества для нашего каталога.
- [ ] **4.5 Из ТЗ («Could»):** §5.8 сравнение урожая по сезонам (в `routes/analytics.js` нет ни
  группировки по годам, ни `EXTRACT` — не начато) · §5.2 профиль участка (частично — шапка вкладки
  «Мой участок», 2026-06-22) · §5.1 поля площадь/тип почвы (`soil_type` бэкенд принимает в
  `POST/PUT /gardens`, UI нет ни на одном клиенте; `area_m2` — колонка в `001_init.sql`, не
  используется даже роутом).
- [~] **4.6 Прочее (не приоритет):** П8 десктоп-layout web (сетка); аналитика без «дней подряд» +
  графики урожая; журнал — фильтр по культуре и удаление **есть**, нет фильтра по типу и редактирования;
  дружелюбные empty/loading/error-состояния.

- [x] **4.8 Сорта культур (справочник + сроки по сортам).** СДЕЛАНО 2026-08-17 — настоящий уровень
  (не текстовая заметка): таблица `crop_varieties`, сорт переопределяет срок урожая культуры и, для
  многолетников, окно съёма (`crops.harvest_doy_start/end`, не существовало вообще). 50/55 культур,
  ~120 сортов, срок сверен по 2+ источникам на каждый. Комбобокс «Сорт» web/Android, таблица сортов
  на SEO-странице культуры. План и подробности —
  `docs/superpowers/plans/2026-08-17-crop-varieties.md`, `session-note.md` (2026-08-17).
- [~] **4.7 Единое снуз/удаление задач дня** (`unified-task-dismiss`) — серверное состояние «отложено/
  удалено» вместо локального Android. План+спека готовы (`docs/superpowers/{plans,specs}/2026-06-27-unified-task-dismiss*`).
  **Сделано**: миграция `054_today_task_dismissals.sql` — и только она (проверено 2026-08-06: грепом
  по `backend/src` строка `tasks/dismiss` встречается лишь в самом файле миграции).
  **Осталось**: `todayLogic.taskKey()` экспорт + фильтрация в `GET /today`, роуты
  `POST /today/tasks/dismiss` + `GET /today/tasks/dismissed`, Android (убрать локальные снуз/делит
  из `TokenStorage`, прямые вызовы API), web (`TaskCard` иконки + `dismissTask`).

## Что реально открыто (сверка 2026-08-06, 4.8 закрыт 2026-08-17)

Технический бэклог короткий: **4.7** (снуз, миграция ждёт месяц), **3.3** (уведомления на web),
**3.2** (управление участками, ⏸ спрос под вопросом), **4.2** (5 фото справочника), **4.5**
(§5.8 / §5.1), **4.6** (десктоп-layout, фильтры журнала), **3.4** (иконки, косметика).
Релизный хвост один: **проверить на живом релизе регистрацию → оплату → чек НПД** (последний
E2E 19.06, с тех пор менялся гейт доступа).

## Рекомендованный старт следующей итерации

**Не фича.** При 22 пользователях и 0 оплат ни один пункт выше не двигает главную метрику, а
воронка не инструментирована — неизвестно, отваливаются люди до участка, до посадки, до упора
в лимит или уже на paywall. Разумный первый шаг — измерить это (события воронки + причина
открытия paywall), затем решать по данным.

Если делать код параллельно — **4.7** (миграция готова, закрывает раздражающее «снуз не
переезжает между устройствами») и **проверка оплаты E2E** (единственный релизный хвост, и он
как раз про деньги).
