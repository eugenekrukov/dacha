# План доработок (приоритизированный)

> Составлен 2026-06-19. Порядок намеренный: **сначала техдолг и исправления, потом новые фичи.**
> Это исполнительный план; широкий бэклог этапа 2+ — `docs/ux-roadmap.md`. Статус задач —
> `summary.md`, история — `session-note.md`.
>
> Обозначения: `[x]` сделано · `[~]` частично · `[ ]` не начато · `⏸` отложено (внешний блокер).

## Фаза 1 — Релиз-блокеры и операционные хвосты

- [x] **1.1 Авто-чек НПД работает end-to-end.** Подтверждено реальным платежом 19.06.2026
  (payment 7 → `registered`). По пути исправлены 2 бага (релей терял токен → `X-Relay-Auth`;
  `scp` затирал секрет → файл `.relay-secret`); секрет `NALOG_RELAY_SECRET` ротирован. См. session-note (2).
- [x] **1.2 Google Play: третий SHA-1** (App signing key) — получен и встроен в ограничение
  Firebase API-key. FCM на Play-сборке регистрируется.
- [x] **1.3 FCM re-register** — на устройстве живой fcm-токен (142 симв.).
- [x] **1.4 Пароль тест-аккаунта** — снято с плана (не важно).
- [x] **1.5 RuStore-ссылка на лендинге** — кнопки ведут на каталог приложения, задеплоено в прод.

## Фаза 2 — Технический долг (код)

- [ ] **2.1 `logout()` не чистит push-токен на клиенте.** `SettingsViewModel.logout()` →
  вызвать `DachaApi.deletePushToken()` (метод есть, мёртвый). Гигиена; серверный фикс уже
  закрывает основной баг привязки токена.
- [ ] **2.2 Удалить код samsung-флейвора** в Android (флейвор не публикуется). Не срочно.
- [x] **2.3 E3 — Android unit-тесты не запускаются.** Исправлено 2026-06-25: причина — Gradle
  тест-воркер на Windows не грузил классы из кириллического `build/`-пути; фикс — редирект `buildDir`
  в `android/build.gradle.kts` на ASCII-путь под не-ASCII проектом. Заодно поправлен всплывший
  реальный баг мока в `TodayViewModelTest`.

## Фаза 3 — Доработка существующего (хвост П4, недостающий UI)

- [x] **3.1 Смена города/региона** — РЕАЛИЗОВАНО. Android `GardenEditScreen` + `CityInputField`;
  web `EditGardenScreen`. Backend `PUT /gardens/:id` был готов.
- [ ] **3.2 Управление участками + переключатель активного сада.** Затрагивает «активный сад» по
  всему приложению — самая объёмная из тройки.
- [ ] **3.3 Настройки уведомлений на web** (на Android тумблеры типов уже есть; опц. время пушей).
- [~] **3.4 Android: эмодзи → иконки** — основные экраны переведены (UI-10, сессия 2026-06-21);
  остаётся хвост на вторичных экранах. Косметика/консистентность.
- [~] **3.5 Грядки + севооборот («план участка») — UI.** ⚠️ Backend ГОТОВ и ЗАДЕПЛОЕН (миграции 052/053,
  `garden_beds` на проде, эндпоинты `GET/POST /gardens/:id/beds`, `PATCH/DELETE /beds/:id` с 3-летней
  историей севооборота по ботаническому семейству), но **клиентского UI нет ни на Android, ни на вебе** —
  ни один клиент не вызывает эти эндпоинты. Фича невидима пользователю. Нужен UI: именованные грядки
  (грунт/теплица), привязка посадки к грядке, подсказка севооборота. Спека: `docs/archive/superpowers/specs/2026-06-27-garden-beds-rotation-design.md`.

## Фаза 4 — Новые фичи (этап 2)

- [x] **4.1 П5. Изображения культур** — СДЕЛАНО: инфраструктура (`crops.image_url`, показ в карточках/
  деталях, lazy-load, кредиты; миграция 047) + фото залиты.
- [ ] **4.2 П5. Долгий хвост справочника** — оставшиеся ~16 записей проблем (фото: iNaturalist/GBIF/свои).
- [x] **4.3 П7. Поиск культур** — Android (`CropsScreen` поиск + чипы) и web (`AddPlantingForm` комбобокс) готовы.
- [—] **4.4 П5. Фото-диагностика болезней** — ОТКЛОНЕНО (низкая точность распознавания). Ни Claude Vision, ни Kindwise не дают приемлемого качества для нашего каталога.
- [ ] **4.5 Из ТЗ («Could»):** §5.8 сравнение урожая по сезонам · §5.2 профиль участка (частично —
  шапка вкладки «Мой участок», 2026-06-22) · §5.1 поля площадь/тип почвы.
- [~] **4.6 Прочее (не приоритет):** П8 десктоп-layout web (сетка); аналитика без «дней подряд» +
  графики урожая; журнал — фильтр по культуре и удаление **есть**, нет фильтра по типу и редактирования;
  дружелюбные empty/loading/error-состояния.

- [~] **4.7 Единое снуз/удаление задач дня** (`unified-task-dismiss`) — серверное состояние «отложено/
  удалено» вместо локального Android. План+спека готовы (`docs/superpowers/{plans,specs}/2026-06-27-unified-task-dismiss*`).
  **Сделано**: миграция `054_today_task_dismissals.sql`. **Осталось**: `todayLogic.taskKey()` экспорт +
  фильтрация в `GET /today`, роуты `POST /today/tasks/dismiss` + `GET /today/tasks/dismissed`, Android
  (убрать локальные снуз/делит из `TokenStorage`, прямые вызовы API), web (`TaskCard` иконки + `dismissTask`).

## Рекомендованный старт следующей итерации

3.5 грядки-UI (бэкенд простаивает) / 4.7 unified-task-dismiss (миграция готова) → 2.1 (гигиена push-токена) → 3.3 (уведомления на web) / 3.2 (управление участками).
