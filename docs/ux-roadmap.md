# UX/продукт — роадмап (этап 2+)

> Бэклог после продакт/UX-ревью 2026-06-15 (аудитория: дачники РФ 40+).
> Этап 1 (П1, П2, П3, П6, П9, П10) — **сделан и задеплоен** (commit `601fc76`).
> Файл — единый источник плана (список задач Claude Code не переживает перезапуск сессии).
>
> **Сверено с кодом и прод-БД 2026-08-06.** Что изменилось по итогам сверки: снят статус
> «не задеплоено» с инвентаря семян и «ждёт релиза» с грядок и хвоста аудита 2026-07-08 (всё
> вошло в vc11/1.0.8, опубликован 03.08); хвост фото справочника — **5 записей, а не ~16**
> (файл сам себе противоречил: в одном пункте «73 из 78 есть», в другом «~16 осталось»).
> Остальные открытые пункты перепроверены запросами и грепом, отметки о проверке — по месту.
>
> ⚠️ **Приоритет читать вместе с бизнес-статусом.** На 2026-08-06: 22 реальных пользователя,
> **0 платящих**, контрольная точка 22.08 (`summary.md` → «Бизнес-статус»). Этот файл отвечает
> на вопрос «что можно сделать», а не «что приблизит первую оплату» — второй вопрос сейчас важнее.

## Незавершённый хвост этапа 1

- [x] **Android: эмодзи → иконки на вторичных экранах.** ✅ 2026-06-25 (`9fa70bb`). TodayScreen
      (hero — переиспользует брендовый `ic_sunflower_png` вместо системного 🌻, чипы погоды, почва,
      предупреждения, 7-дневный прогноз, пусто), Analytics (стрик, чек-лист онбординга), Harvest,
      Crops/CropSections (морозостойкость/многолетник/поиск), Guide+GuideViewModel (`guideKindIcon`
      теперь возвращает `ImageVector`), CreateGarden, OnboardingCrops, VerifyEmail, CityInputField,
      TutorialIntroScreen, CalendarViewModel (избыточные эмодзи-префиксы убраны — иконка уже
      рисуется отдельно через `eventStyle`). Заодно дополнительно найденные: лента «Мой участок»
      (вехи сезона), предупреждение на «Посадках», экран сброса пароля. Не трогали: статус-строки
      с префиксом «✓ GPS-координаты…» в Garden*ViewModel/Screen (текстовый чек-марк, не цветной
      эмодзи — рефактор в Icon+Text требует менять структуру данных за небольшую визуальную выгоду).
      Проверено: `:app:compileGplayDebugKotlin` + `:app:compileGplayDebugUnitTestKotlin` — BUILD
      SUCCESSFUL.

## Этап 2 — приоритетное

- [~] **П4. Переработка Настроек** (обе платформы):
  - [x] Смена города/региона — РЕАЛИЗОВАНО (Android `GardenEditScreen` + `CityInputField`, web `EditGardenScreen`).
  - [ ] Управление участками + переключатель активного (у пользователя несколько садов, switcher отсутствует). *(подфича D — отдельный цикл; затрагивает «активный сад» по всему приложению)*
        ⚠️ **2026-08-14, проверить перед тем, как делать** — нужен реальный спрос: посчитать по БД,
        сколько зарегистрированных аккаунтов создали больше одного участка (`SELECT
        user_id, count(*) FROM gardens GROUP BY user_id HAVING count(*) > 1`). НЕ строить вслепую.
  - [—] Настройки уведомлений на **web** — **решено не делать** (2026-08-14, владелец). На Android
        тумблеры типов уже есть.
  - [x] **Удаление аккаунта** — обязательно для Google Play / RuStore Data Safety. ✅ 2026-06-15: `DELETE /auth/me` (hard delete + анонимизация `payments`) + UI (web/Android). В проде.
  - [x] Смена пароля/email, «О приложении»/версия, оферта/политика, поддержка. ✅ 2026-06-15: `PATCH /auth/password`, смена email verify-first (`POST /auth/change-email` + `/auth/confirm-email-change`, `users.pending_email`, миграция 036), секция «О приложении». В проде (backend+web); Android в `main`, публикует пользователь. Спек/план: `docs/superpowers/{specs,plans}/2026-06-15-account-security-settings*`.
- [~] **П5. Изображения** (апгрейд №1 по ценности для 40+):
  - [x] **Фото культур — ЗАЛИТЫ 2026-08-06 (`65dfbae`), 55 из 55.** Файлы
        `web/public/media/crops/<slug>.jpg` (900 px, 8.7 MB), ссылки и атрибуция — миграция 064.
        Источник Wikimedia Commons, только свободные лицензии (GFDL отсеяна: требует прикладывать
        полный текст лицензии). Подбор воспроизводим: `node backend/scripts/fetch-crop-images.js`,
        источники по каждому кадру — `backend/scripts/crop-images.sources.json`.
        Проверено на проде: `/crops` отдаёт `image_url`+`image_credit` у 55/55, файлы отдаются
        как `image/jpeg`. Теперь фото культур **можно** заявлять в ASO-текстах.
        > Все кадры просмотрены глазами: автоматический подбор давал ботанические гравюры,
        > сканы каталогов семян XIX века, семена вместо корнеплода и цветение вместо плодов.
        > Если будете менять фото — смотрите итог, а не доверяйте выдаче поиска.
  - [x] Backend: поле `crops.image_url` + показ в карточках/деталях, lazy-load, кредиты автора (миграция 047). ✅
  - [~] **Долгий хвост: 76 из 78 (2026-08-06, миграция 065).** Закрыты угловатая пятнистость
        (огурец), фомопсис (баклажан) и недостаток бора. Кадры сверялись с привязкой записи
        к культурам, чтобы болезнь показывалась на той же культуре.
        **Осталось 2, и они не закрываются с Wikimedia Commons:**
        - `fomoz-suhaya-gnil` (морковь, свёкла) — на Commons есть только Phoma на всходах шпината
          и Phoma lingam на капусте; серо-коричневых пятен на корнеплодах они не показывают.
        - `lukovyy-skrytnohobotnik` (лук) — нет Ceutorhynchus suturalis, только другие виды рода.
        Нужны **свои снимки** либо iNaturalist/GBIF с их лицензиями (другой пайплайн атрибуции).
        > Правило: для справочника болезней неверное фото хуже отсутствующего — оно ведёт
        > к ложному диагнозу. Пустое место честнее «похожей» картинки.
  - [—] **Фото-диагностика болезней — ОТКЛОНЕНО** (низкая точность распознавания). Прогон вариантов
        (Kindwise plant.health, Claude Vision и др.) показал недостаточное качество для нашего каталога
        ягод/цветов/деревьев — ложные диагнозы хуже отсутствия фичи для аудитории 40+. Не возвращаться
        без принципиально нового движка.
- [x] **П7. Поиск культур**: Android готово (`CropsScreen` поиск + чипы категорий, `OnboardingCropsScreen`).
      Web — РЕАЛИЗОВАНО 2026-06-25 (`AddPlantingForm`): комбобокс с поиском + фильтром по категориям,
      зеркало Android.
- [x] **Грядки + севооборот («план участка»)** — РЕАЛИЗОВАНО (2026-07-01), **у пользователей с
      vc11/1.0.8 (опубликован 03.08)**. Бэкенд (миграции 052/053 +
      GRANT 055 для `dacha_user`) + UI на web (в проде) и Android: поле «Место»
      с инлайн-созданием/переименованием/удалением грядок (грунт/теплица), привязка посадки, мягкая
      подсказка севооборота по семейству за 3 года, просмотр/правка «Места» в карточке. Планы:
      `docs/superpowers/plans/2026-06-30-garden-beds-{web,android}-ui.md`. Follow-up (2026-07-01):
      «Условия» перенесены из формы создания в карточку, на вебе добавлен выбор способа посадки.
- [ ] **Перечень посадок на конкретной грядке** — в план (владелец, 2026-08-14). (добавлено 2026-07-28) — сейчас связь
      посадка → грядка одностороння (в карточке посадки видно её «Место»), обратного списка
      «что растёт на этой грядке» нет. Нужен вид/раздел с грядкой и списком привязанных к ней
      посадок — полезно для планирования севооборота (за 3 года по грядке, не по всему участку).
      *Подтверждено 2026-08-06: в `routes/beds.js` нет ни одного упоминания `bed_id` — обратной
      выборки нет даже на бэкенде.*
- [x] **Инвентарь семян** (реализовано 2026-07-29, идея с разбора конкурента easyDacha).
      У дачника коробка с пакетиками, он не помнит, что уже куплено, и покупает второй раз.
      Сделано: таблица `seeds` (миграция 060), роуты `/seeds` (CRUD + фото пакетика отдельным
      multipart-эндпоинтом), экран «Мои семена» в вебе (`/seeds`, меню «Ещё») и на Android
      (`Screen.Seeds`, таб «Ещё»). Проверка срока годности — флаги `expired` / `expires_this_year`
      считает SQL, клиенты только красят; просроченные поднимаются вверх списка.
      ✅ **Задеплоено** (проверено 2026-08-06: таблица `seeds` на проде, `GET /seeds` → 200,
      экран вошёл в vc11/1.0.8). Прежняя пометка «не задеплоено» держалась неделю после факта.
- [ ] **Инвентарь семян — список покупок** (2026-08-14: в план). Запланированные посадки, которым
      нет семян в инвентаре — требует связи `seeds.crop_id` → `crops` (сейчас культура хранится
      свободным текстом ради цветов, которых нет в справочнике).
- [ ] Инвентарь семян — **количество пакетиков** и **отметка «посеяно»** (не приоритет, ждёт своей очереди).
- [—] **Распознавание текста с пакетика (OCR) — отложено** (владелец решил не тащить OCR, пока
      не видно, что инвентарём вообще пользуются; подтверждено 2026-08-14). Оценка на будущее:
      либо vision-модель по API (надёжно на кириллице и кривых шрифтах, нужен ключ +
      ~0.2 коп/фото), либо tesseract.js на VPS (бесплатно, но на глянцевом пакетике качество
      посредственное).
- [ ] **Даты заморозков как явная механика** (добавлено 2026-07-29, оттуда же). Климат-зона
      уже участвует в расчётах, но пользователь её не видит. Показывать явно: «последние
      весенние заморозки в вашем регионе — ~25 мая; сеять перец на рассаду 10 марта».
      Осязаемо, объясняет, откуда взялись сроки, и хорошо продаётся на скриншотах.
      Проверить, что уже есть в данных зоны (`getZoneDayOffset`), и чего не хватает для
      показа конкретной даты, а не сдвига.
- [x] **Агрономические данные культур — ЗАКРЫТО 2026-07-30.** Пункт заводился как «дописать
      23 недостающие культуры» по счёту сид-миграций в репо, но проверка прода показала другое:
      агрономия была заполнена у 54 культур из 55, просто примерно у 24 из них — **только в проде,
      мимо git**. То есть писать было нечего, а вот терять — было что: БД с нуля по миграциям
      давала бедный справочник. Прод-значения зафиксированы миграцией
      `061_seed_crops_agronomy_from_prod.sql`, вручную дописана единственная реально пустая
      культура — Петуния. Сейчас 55/55. Урок: считать покрытие данных по БД, а не по миграциям.

- [ ] **Схема посадки культур (`crops`) — предпосылка ко всему «карточному» планированию** —
      в план вместе с подсказкой вместимости грядки (владелец, 2026-08-14).
      (добавлено 2026-07-29). Сейчас в `crops` нет ни расстояния между растениями, ни плотности:
      есть сроки, полив, семейство, совместимость. Без этих данных невозможны ни подсказка
      вместимости грядки, ни визуальная карта. Работа не программистская, а агрономическая:
      внести схему (в ряду × между рядами, либо шт/м²) для 55 культур. **Делать это первым**,
      независимо от решения по карте.
      *Подтверждено 2026-08-06: в `crops` нет ни одной колонки вида `%spacing%`/`%density%`/
      `%plants_per%` — не начато.*
- [ ] **Подсказка вместимости грядки** (добавлено 2026-07-29) — после схемы посадки: у грядки
      появляются размеры (ширина × длина), в карточке считается «на грядке 1×3 м поместится
      ~6 кустов томата». Даёт основную пользу «карты участка» без самой карты.
      Оценка: ~15–20 ч на обе платформы. Строить после проверки спроса.
      *Подтверждено 2026-08-06: в `garden_beds` только `id, garden_id, name, type, created_at` —
      размеров нет, нужна миграция.*
> **Рекомендованный порядок по «планированию участка» (2026-07-29).** Не строить карту, взять
> её пользу дешевле и по шагам: **1)** схема посадки в `crops` (агрономические данные, без них
> ничего не считается) → **2)** размеры грядки + подсказка вместимости («на грядке 1×3 м
> поместится ~6 кустов томата») → **3)** посмотреть, просят ли пользователи именно план участка,
> и только тогда возвращаться к карте. Основная ценность карты для дачника — ответ на вопросы
> «сколько влезет» и «что здесь росло раньше»; второе уже закрыто севооборотом, первое даёт
> шаг 2 за ~15–20 ч вместо ~80–110. Та же логика, по которой отложен переключатель участков:
> не строить вслепую.

- [—] **Визуальная карта участка (drag-n-drop грядок)** — оценена 2026-07-29 по следам разбора
      конкурента easyDacha, **строить не рекомендуется на текущем этапе**. Требует: схему посадки
      (55 культур вручную), геометрию грядок (размеры + координаты + поворот, миграция и API),
      редактор с перетаскиванием и зумом отдельно на вебе (SVG/Canvas) и на Android
      (Compose Canvas + мультитач-жесты), раскладку растений внутри грядки, разрешение конфликтов
      при офлайн-правках. Итого ~80–110 ч, то есть 5–7 месяцев при текущем темпе 3–5 ч/нед.
      Отдельный риск: рисование участка пальцем на телефоне неудобно аудитории 35+ — у easyDacha
      приложение в первую очередь под iPad. Вернуться к вопросу, только если вместимость грядки
      окажется востребованной и пользователи начнут просить именно план участка.

## Этап 2 — из ТЗ (`docs/ТЗ.pdf`, приоритет «Could»)

- [—] §5.8 — сравнение урожая по сезонам. **Отложено** (владелец, 2026-08-14). Изначально
      (2026-06-25): тема хорошая, но нужно сначала просчитать объём работы (данных по урожаю
      уже достаточно собирается через `/harvests`?) — так и не просчитано, оценка не приоритет.
- [~] §5.2 — профиль участка (частично: шапка вкладки «Мой участок», 2026-06-22).
- [ ] §5.1 — поля «площадь» и «тип почвы». ⚠️ **2026-06-25, открытый вопрос владельца**: на что это
      будет влиять ЕЖЕДНЕВНО и какая от этого польза пользователю — пока только метаданные без
      функциональной привязки (не двигают рекомендации/расписание). Не строить, пока не найдена
      конкретная польза (например: тип почвы → частота полива/выбор культур).

## Этап 2 — прочее из ревью (не приоритет)

- [—] **П8.** Десктоп-layout web — сетка вместо узкой центрированной колонки. **Решено не делать**
      (владелец, 2026-08-14).
- [ ] **Аналитика: убрать метрику «дней подряд»** — в план (владелец, 2026-08-14). Это `streak`
      из `backend/src/routes/analytics.js`, показывается в статистике профиля
      (`android/.../ui/analytics/AnalyticsScreen.kt:117`, значение `summary.streak`) — та же
      метрика, что во вкладке «Статистика». Рационал из ревью (`docs/ui-review-2026.md` UI-9,
      `docs/product-research-2026.md` U6): «честные метрики» — streak-счётчики уместны в
      привычка-формирующих приложениях с ежедневным циклом (Duolingo), а уход за огородом по
      природе нерегулярный — пропущенный день не означает «провал», метрика создаёт ложное
      давление вместо честного отражения активности. Простые графики урожая по культурам/сезонам
      и другие графики активности — отдельная тема, не часть этой задачи.
- [ ] Журнал действий: фильтр по культуре + удаление (long-press) **есть**; осталось фильтр по типу +
      редактирование. В план (владелец подтвердил 2026-06-25, повторно 2026-08-14).
      *Подтверждено 2026-08-06: в `web/src/screens/JournalScreen.tsx` есть только `cropFilter`,
      фильтра по `action_type` и правки записи нет.*
- [—] Тон/состояния: empty/loading/error-состояния, дружелюбные пустые экраны. **Решено не делать**
      (владелец, 2026-08-14).

## Этап 2 — хвост кросс-платформенного UI/UX-аудита (2026-07-08)

> Источник: живой прогон web (превью) + Android (эмулятор, rustore-debug) по сценариям
> онбординг → «Сегодня» → лог действия → лента → справочник → paywall. Сделанное в этот заход
> см. в `session-note.md` (2026-07-08). Ниже — что осталось.

**Сделано (для контекста, не переоткрывать):** советы дня свёрнуты до 3 + «показать ещё» (обе
платформы); порядок/иконка табов web = Android; контраст hero поднят scrim'ом (обе платформы);
бейдж «нужен уход» на «Посадках» в web; заголовок секции задач унифицирован («Задачи на сегодня»);
перестройка «Профиля» (личный раздел + таб «Ещё») на web и Android.

**Недоделки:**
- [ ] **Контраст hero — мелкие вторичные подписи.** Крупный текст (участок/темп) после scrim
      проходит AA; но самые мелкие подписи на светлом крае градиента (город, «Переменная облачность»)
      всё ещё у порога 4.5:1. Либо усилить scrim (притемнит «солнечность»), либо поднять вес/размер
      этих подписей.
- [x] **«Сегодня» — тревожная перегрузка задач — ЗАКРЫТО 2026-08-08 (`954e74b`).** Ступень
      срочности считает бэкенд (`urgencyLevel` в `utils/todayLogic.js`), поле `urgency` в каждой
      задаче `/today`: `critical` (заморозки) · `late` (просрочка > 7 дн., красный) · `soon`
      (1–7 дн., приглушённый) · `normal`. Клиенты только красят — web (`URGENCY_PILL`) и
      Android (`overdueBadgeColors`).
      > Порог сначала поставили 3 дня — на живых данных прода (демо-участок) в `late` попало
      > 6 задач из 7, экран остался таким же красным. Подняли до недели: 3 late / 4 soon.
      > Мораль: проверять на реальных данных, а не только на тестах с придуманными числами.
- [ ] **Контраст текста на фото-подложке (WCAG AA)** — проверить (владелец, 2026-08-14). Не проверен
      на `PhotoDiary` и карточках ленты (флаг из брифа UI-ревью). Проверить читаемость подписей
      поверх фото.
- [ ] **Тач-таргеты ≥48dp** — проверить (владелец, 2026-08-14). Полный аудит не проводился;
      точечно проверить иконки-кнопки (карандаш «участок» в hero, «×»-чипы в шторке лог-действия).
- [ ] **Унификация карточек между платформами** (косметика, консистентность):
  - карточка задачи: Android = красная пилюля «+15д» + плитка-иконка + шеврон; web = текст «19 дн.»
    + «Подробнее →» — разный аффорданс одного объекта;
  - карточки советов: Android = цветные плитки-иконки + тонированный фон + свайп; web = белая
    карточка + «×»;
  - прогноз погоды: Android = аккордеон «на 7 дней»; web = 7 дней инлайн всегда;
  - hero-заголовок: Android = generic «Сегодня на даче» + влажность почвы; web = имя участка + город.

---

## Сводка открытого (сверка 2026-08-14)

Чтобы не вычитывать файл целиком. Порядок — по стоимости отсутствия, не по объёму работы.
Приоритизация ниже — решение владельца 2026-08-14.

**Дёшево и заметно пользователю:**
1. ~~Фото 55 культур~~ — ✅ сделано 2026-08-06.
2. ~~5 фото справочника~~ — ✅ 3 из 5 закрыты 2026-08-06 (76/78). Оставшиеся 2 требуют
   **своих снимков**: на Commons подходящих кадров нет (см. П5 выше).
3. ~~Градация срочности задач~~ — ✅ закрыто 2026-08-08.

**В план (будем делать):**
4. **Перечень посадок по грядке** (нет даже на бэкенде).
5. **Фильтр журнала по типу + правка записи**.
6. **Инвентарь семян — список покупок**.
7. **Убрать «дней подряд» из статистики** (`streak` в `backend/src/routes/analytics.js`).
8. **Схема посадки в `crops`** → **размеры грядки + вместимость**. Порядок шагов зафиксирован выше,
   ни один не начат (колонок нет ни там, ни там).

**Проверить перед решением:**
9. **Переключатель участков** — посчитать по БД долю аккаунтов с >1 участком, прежде чем строить.
10. **Контраст текста на фото-подложке и тач-таргеты ≥48dp** — аудит доступности не проводился.

**Решено не делать (2026-08-14):**
11. Уведомления на web · десктоп-layout (П8) · empty/error-состояния.

**Отложено сознательно (не брать без спроса):**
12. OCR пакетика семян · §5.1 поля участка (площадь/тип почвы) · §5.8 сравнение урожая по сезонам.

**Отклонено:** визуальная карта участка (~80–110 ч) · открытая фото-диагностика болезней общего вида
(закрытый вариант по своему справочнику культуры — реализован и в проде, см. F2 в `summary.md`).
- [x] **Android-релиз шипнутого 2026-07-08** — ✅ доехал до пользователей в vc11/1.0.8
      (опубликован в Google Play и RuStore 03.08). Контраст hero + перестройка «Профиля»/«Ещё».
