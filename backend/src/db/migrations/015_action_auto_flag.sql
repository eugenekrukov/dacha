-- Флаг авто-заметки у действия: заметка была подставлена автоматически (имя care-задачи/
-- удобрения при логировании из задачи дня), а не введена пользователем. Нужен, чтобы
-- надёжно скрывать дублирующую заметку в журнале вместо хрупкой строковой эвристики на клиенте.
ALTER TABLE action_logs ADD COLUMN IF NOT EXISTS auto BOOLEAN NOT NULL DEFAULT false;
