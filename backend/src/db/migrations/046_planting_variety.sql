-- Сорт посадки (по просьбе тестеров): свободный текст, опционально.
-- Отдельная колонка (а не notes) — чтобы потом фильтровать/сравнивать урожай по сортам.
ALTER TABLE plantings ADD COLUMN IF NOT EXISTS variety VARCHAR(120);
