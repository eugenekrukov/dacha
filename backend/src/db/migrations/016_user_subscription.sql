-- Серверная отметка активной подписки (источник истины по подписке — RuStore на клиенте,
-- сервер хранит «до какого момента подписка подтверждена»). Клиент при старте синхронизирует
-- статус (POST /auth/subscription); сервер продлевает окно. Нужно для серверного гейта
-- платных действий после окончания триала.
ALTER TABLE users ADD COLUMN IF NOT EXISTS subscription_until TIMESTAMPTZ;
